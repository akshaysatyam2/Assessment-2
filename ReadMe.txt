You need to run MySQL commands in "SQL Commands" file before running Project.

Note:- Every pc has different database stored, hence, display may differ.
     - If there exists a precreated database named "Task", then drop it with "DROP DATABASE Task;"

For Login:-
user= "admin"
password="admin"